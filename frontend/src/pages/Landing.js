import React from 'react';
import { Typography } from '@mui/material';

const Landing = () => (
  <div>
    <Typography variant="h4" gutterBottom>
      Welcome to the Bioinformatics Insights Tool
    </Typography>
    <Typography variant="body1">
      Use the sidebar to explore data-driven insights from the breast cancer RNA-seq dataset.
    </Typography>
  </div>
);

export default Landing;
